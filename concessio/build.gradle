apply plugin: 'com.android.application'
buildscript {
    repositories {
        maven { url  "http://dl.bintray.com/android/android-utils" }
        jcenter()
        mavenCentral()
        maven { url 'https://maven.fabric.io/public' }
    }
    dependencies { //2.0.0-beta6
        classpath 'com.android.tools.build:gradle:2.1.0'
        classpath 'io.fabric.tools:gradle:1.+'
    }
}
apply plugin: 'io.fabric'

repositories {
    maven { url  "http://dl.bintray.com/android/android-utils" }
    jcenter()
    mavenCentral()
    maven { url 'https://maven.fabric.io/public' }
}

android {
    compileSdkVersion 23
    buildToolsVersion '23.0.2'


    signingConfigs {
        concessio {
            keyAlias 'concessio'
            keyPassword 'concessio'
            storeFile file('/Users/rhymartmanchus/Documents/workspace/imonggosdk/concessioofficial.jks')
            storePassword 'concessioofficial'
        }
    }

    defaultConfig {
        applicationId "net.nueca.concessio.official"
        minSdkVersion 16
        targetSdkVersion 23
        versionCode 7
        versionName "1.3.1"
    }
    buildTypes {
        release {
            signingConfig signingConfigs.concessio
            debuggable false
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
//    splits {
//        abi {
//            enable true
//            reset()
//            include 'x86', 'armeabi-v7a'
//            universalApk true
//        }
//    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:23.2.0'
    compile 'com.android.support:design:23.2.0'
    compile 'com.android.support:support-v4:23.2.0'
    compile project(':concessioengine')
    compile('com.crashlytics.sdk.android:crashlytics:2.5.6@aar') {
        transitive = true;
    }
}
