<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.nueca.imonggosari" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-feature android:name="android.hardware.usb.accessory" />
    <!-- Show Alert Dialog in Service -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.Concessio.NoActionBar" >
        <service
            android:name="net.nueca.imonggosdk.swable.ImonggoSwable"
            android:enabled="true" />
        <service
            android:name="net.nueca.imonggosdk.operations.sync.SyncModules"
            android:enabled="true" />
        <service
            android:name="com.generalscan.bluetooth.connect.ATService"
            android:enabled="true" />

        <activity
            android:name=".activities.SariLogin"
            android:label="@string/app_name"
            android:theme="@style/LoginTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="50e2100f1a801357b9d65a4f7f38551db0df28ce" />

        <activity
            android:name=".activities.SariMain"
            android:label="@string/title_activity_sari_main"
            android:parentActivityName=".activities.SariLogin"
            android:screenOrientation="landscape" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.SariLogin" />
        </activity>
    </application>

</manifest>
